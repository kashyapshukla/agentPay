[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentpay"
version = "1.0.0"
authors = [{name = "AgentPay Authors"}]
description = "B2A Infrastructure SDK for Autonomous Agents"
dependencies = [
    "httpx>=0.28.1"
]
